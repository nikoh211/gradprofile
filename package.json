{
  "scripts": {
    "build": "NEXT_TELEMETRY_DISABLED=1 next build",
    "lint": "next lint --fix",
    "start": "concurrently \"next start\" \"cd backend && node server.js\"",
    "deploy": "docker-compose up -d --build"
  },
  "dependencies": {
    "@pdftron/pdfjs-express-viewer": "^8.7.5",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "formidable": "^3.5.2",
    "mammoth": "^1.8.0",
    "multer": "^1.4.5-lts.1",
    "pdf-parse": "^1.1.1",
    "concurrently": "^8.0.0"
  }
}
